import React from "react";

function EditBtn({}) {
  const handleEditModeClick: React.MouseEventHandler = () => {};

  return (
    <button onClick={handleEditModeClick} type="button">
      EditBtn
    </button>
  );
}

export default EditBtn;
